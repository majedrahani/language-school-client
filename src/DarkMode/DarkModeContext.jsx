import React, { createContext } from 'react';



export const DarkModeSystemContext = createContext();
const DarkModeContext = () => {
    return (
        <div>
            
        </div>
    );
};

export default DarkModeContext;